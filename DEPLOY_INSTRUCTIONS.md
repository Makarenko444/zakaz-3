# Инструкции по деплою миграции 024

## ✅ Что уже сделано

Все изменения закоммичены и запушены в ветку:
```
claude/merge-address-tables-01JnCEwCnPwwHL8xCnL9snHo
```

Последний коммит: Обновлен AddressLinkWizard (исправлена ошибка компиляции)

## 🔧 Как задеплоить

### Вариант 1: Через GitHub UI (рекомендуется)

1. Откройте GitHub: https://github.com/Makarenko444/zakaz-3
2. Нажмите "Compare & pull request" для ветки `claude/merge-address-tables-01JnCEwCnPwwHL8xCnL9snHo`
3. Создайте Pull Request
4. Смержите PR в main
5. На сервере выполните: `pr-main`

### Вариант 2: Через командную строку

```bash
# Переключитесь на ветку с изменениями
git checkout claude/merge-address-tables-01JnCEwCnPwwHL8xCnL9snHo

# Создайте новый коммит с мержем в main (fast-forward)
git push origin claude/merge-address-tables-01JnCEwCnPwwHL8xCnL9snHo:main

# Задеплойте на сервер
pr-main
```

### Вариант 3: Локальный мерж

```bash
# Создайте локальную ветку main если её нет
git checkout -b main origin/main

# Смержите изменения
git merge claude/merge-address-tables-01JnCEwCnPwwHL8xCnL9snHo

# Запушьте в main
git push origin main

# Задеплойте
pr-main
```

## 📊 Изменения в этой ветке

- ✅ SQL миграция 024
- ✅ Обновлены TypeScript типы
- ✅ Обновлены API endpoints
- ✅ Обновлены UI компоненты
- ✅ Исправлена ошибка компиляции в AddressLinkWizard

## ⚠️ После деплоя

**ОБЯЗАТЕЛЬНО** применить SQL миграцию в Supabase:

1. Откройте Supabase Dashboard
2. SQL Editor → New query
3. Скопируйте содержимое `database/migrations/024_merge_addresses_into_nodes.sql`
4. Выполните запрос

## 🧪 Проверка

После деплоя и миграции проверьте:

```sql
-- Структура таблицы
\d zakaz_nodes

-- Данные перенесены
SELECT presence_type, COUNT(*) FROM zakaz_nodes GROUP BY presence_type;

-- Ссылки обновлены
SELECT COUNT(*) FROM zakaz_applications WHERE node_id IS NOT NULL;
```

И в UI:
- Создание заявки
- Привязка адреса через мастер
- Фильтр по адресу в списке заявок
