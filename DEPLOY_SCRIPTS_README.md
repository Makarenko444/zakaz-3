# Скрипты быстрого деплоя

## Доступные скрипты

### pr-zakaz3.sh
Деплой на тестовый сервер **zakaz3.tomica.ru**

### pr-zakaz2.sh
Деплой на **БОЕВОЙ** сервер **zakaz2.tomica.ru**

## Первоначальная настройка

### 1. Узнайте имена PM2 процессов

Выполните на сервере:
```bash
pm2 list
```

Вы увидите список всех запущенных процессов. Запомните имена процессов для zakaz2 и zakaz3.

### 2. Настройте скрипты

Отредактируйте файлы и укажите правильные имена процессов:

**В pr-zakaz3.sh:**
```bash
PM2_PROCESS_NAME="zakaz-3"  # Замените на имя вашего процесса для zakaz3
```

**В pr-zakaz2.sh:**
```bash
PM2_PROCESS_NAME="zakaz-2"  # Замените на имя вашего процесса для zakaz2
```

### 3. Установите алиасы

Для удобного использования добавьте алиасы в `~/.bashrc`:

```bash
# Добавьте алиасы
echo "alias pr-zakaz3='cd ~/projects/zakaz-3 && ./pr-zakaz3.sh'" >> ~/.bashrc
echo "alias pr-zakaz2='cd ~/projects/zakaz-2 && ./pr-zakaz2.sh'" >> ~/.bashrc

# Перезагрузите конфигурацию
source ~/.bashrc
```

**Важно:** Обратите внимание что алиасы используют разные директории:
- `pr-zakaz3` → `~/projects/zakaz-3`
- `pr-zakaz2` → `~/projects/zakaz-2`

## Использование

После установки алиасов можно использовать простые команды:

```bash
# Деплой на zakaz3 (тестовый)
pr-zakaz3

# Деплой на zakaz2 (боевой)
pr-zakaz2
```

## Что делают скрипты

1. ✅ Переходят в директорию проекта
2. 📥 Получают последние изменения из GitHub (main)
3. 📦 Устанавливают зависимости
4. 🗑️ Очищают старый билд
5. 🔨 Собирают проект
6. 🔄 Перезапускают PM2 процесс
7. ✅ Выводят ссылку на сайт

## Важно

- **pr-zakaz2** деплоит на **БОЕВОЙ** сервер! Используйте с осторожностью
- **pr-zakaz3** деплоит на тестовый сервер для проверки изменений
- После деплоя не забудьте очистить кэш браузера (Ctrl+Shift+R)

## Миграция от старого алиаса pr-main

Если у вас был старый алиас `pr-main`, удалите его из `~/.bashrc`:

```bash
# Откройте файл в редакторе
nano ~/.bashrc

# Найдите и удалите строку:
# alias pr-main='cd ~/zakaz-3 && ./pr-main.sh'

# Или удалите автоматически:
sed -i '/alias pr-main=/d' ~/.bashrc

# Перезагрузите конфигурацию
source ~/.bashrc
```

## Настройка PM2 процессов

По умолчанию скрипты используют следующие имена процессов:
- zakaz3: `zakaz-3` (измените в pr-zakaz3.sh если нужно)
- zakaz2: `zakaz-2` (измените в pr-zakaz2.sh если нужно)

Если скрипт не найдет процесс, он покажет список всех доступных PM2 процессов.
