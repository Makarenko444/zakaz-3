# Optimize npm for deployment
# Use faster algorithms
prefer-offline=true
audit=false
fund=false
# Reduce concurrent downloads to avoid overloading the server
maxsockets=3
# Use cache more aggressively
cache-min=9999999
# Disable progress for faster CI/CD
progress=false
