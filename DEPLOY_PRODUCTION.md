# Инструкция по установке скрипта деплоя на боевой сервер zakaz2.tomica.ru

## 📋 Шаг 1: Подключение к серверу

```bash
ssh makarenko@zakaz2.tomica.ru
# или через IP, если настроен
```

## 📋 Шаг 2: Переход в директорию проекта

```bash
cd ~/zakaz-3
```

## 📋 Шаг 3: Создание скрипта деплоя

Создайте файл `pr-main.sh`:

```bash
nano pr-main.sh
```

Скопируйте содержимое файла `pr-main-production.sh` из репозитория в этот файл.

Или можно скачать напрямую из GitHub (если файл закоммичен):

```bash
# Убедитесь, что вы на ветке main
git checkout main
git pull origin main

# Скопируйте скрипт
cp pr-main-production.sh pr-main.sh
```

## 📋 Шаг 4: Настройка прав на выполнение

```bash
chmod +x pr-main.sh
```

## 📋 Шаг 5: Настройка PM2 процесса (если не настроен)

Проверьте текущие процессы PM2:

```bash
pm2 list
```

Если процесс `zakaz-3` не найден, создайте его:

```bash
# Соберите проект
npm install
npm run build

# Запустите через PM2
pm2 start npm --name zakaz-3 -- start

# Сохраните конфигурацию PM2
pm2 save

# Настройте автозапуск при перезагрузке
pm2 startup
# Выполните команду, которую предложит PM2
```

## 📋 Шаг 6: Настройка имени PM2 процесса (если отличается)

Если ваш процесс PM2 называется по-другому, отредактируйте скрипт:

```bash
nano pr-main.sh

# Найдите строку:
PM2_PROCESS_NAME="zakaz-3"

# Замените на нужное имя, например:
PM2_PROCESS_NAME="zakaz-2"
# или
PM2_PROCESS_NAME="production"
```

## 📋 Шаг 7: Первый запуск

Запустите скрипт деплоя:

```bash
./pr-main.sh
```

## 🔧 Использование

После установки используйте скрипт для деплоя:

```bash
cd ~/zakaz-3
./pr-main.sh
```

## 📊 Полезные команды PM2

```bash
# Просмотр логов
pm2 logs zakaz-3

# Просмотр логов в реальном времени
pm2 logs zakaz-3 --lines 100

# Статус процессов
pm2 status

# Перезапуск вручную
pm2 restart zakaz-3

# Остановка
pm2 stop zakaz-3

# Мониторинг
pm2 monit
```

## 🚨 Решение проблем

### Проблема: "Ошибка: директория проекта не найдена"

**Решение:** Проверьте путь к проекту. Если проект находится не в `~/zakaz-3`, отредактируйте скрипт:

```bash
nano pr-main.sh

# Найдите строку:
PROJECT_DIR=~/zakaz-3

# Замените на правильный путь, например:
PROJECT_DIR=/home/makarenko/projects/zakaz-3
```

### Проблема: Конфликт в package-lock.json

**Решение:** Скрипт автоматически сохранит локальные изменения в stash. Если конфликт возник:

```bash
# Посмотрите изменения в stash
git stash list

# Сбросьте package-lock.json
git checkout -- package-lock.json

# Запустите деплой снова
./pr-main.sh
```

### Проблема: PM2 процесс не найден

**Решение:**

```bash
# Проверьте список процессов
pm2 list

# Если процесс называется по-другому, узнайте его имя
# и обновите в скрипте переменную PM2_PROCESS_NAME

# Или запустите новый процесс
pm2 start npm --name zakaz-3 -- start
pm2 save
```

### Проблема: Ошибка сборки (npm run build)

**Решение:**

```bash
# Очистите node_modules и кэш
rm -rf node_modules .next
npm cache clean --force
npm install
npm run build
```

## 🔐 Безопасность

- Скрипт работает только с веткой `main`
- Перед применением изменений запрашивается подтверждение если вы не на ветке main
- Локальные изменения сохраняются в stash перед pull
- При ошибке сборки деплой останавливается

## 📝 Логирование

Все действия скрипта выводятся в консоль с цветовой индикацией:
- 🔵 Синий - информационные сообщения
- 🟡 Желтый - предупреждения
- 🟢 Зеленый - успешные операции
- 🔴 Красный - ошибки

## 🎯 Рекомендации

1. **Перед деплоем:** Убедитесь, что изменения протестированы на dev сервере (zakaz3.tomica.ru)
2. **После деплоя:** Проверьте работу сайта в браузере
3. **Мониторинг:** Следите за логами PM2 первые несколько минут после деплоя
4. **Резервное копирование:** Периодически делайте бэкап базы данных

## 📞 Контакты

При возникновении проблем:
- Проверьте логи: `pm2 logs zakaz-3`
- Проверьте статус: `pm2 status`
- Проверьте логи сервера: `journalctl -u nginx -f` (если используется nginx)
