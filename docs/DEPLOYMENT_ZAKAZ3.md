# Инструкция по развертыванию сервера zakaz3.tomica.ru

**Дата создания:** 2 декабря 2025
**Версия:** 1.0
**Сервер:** zakaz3.tomica.ru (Development)

---

## 📋 Содержание

1. [Обзор](#обзор)
2. [Требования](#требования)
3. [Шаг 1: Обновление системы](#шаг-1-обновление-системы)
4. [Шаг 2: Установка Node.js через nvm](#шаг-2-установка-nodejs-через-nvm)
5. [Шаг 3: Установка PM2](#шаг-3-установка-pm2)
6. [Шаг 4: Установка Git](#шаг-4-установка-git)
7. [Шаг 5: Установка Docker](#шаг-5-установка-docker)
8. [Шаг 6: Клонирование проекта](#шаг-6-клонирование-проекта)
9. [Шаг 7: Настройка переменных окружения](#шаг-7-настройка-переменных-окружения)
10. [Шаг 8: Установка зависимостей и сборка](#шаг-8-установка-зависимостей-и-сборка)
11. [Шаг 9: Запуск через PM2](#шаг-9-запуск-через-pm2)
12. [Шаг 10: Настройка Caddy для HTTPS](#шаг-10-настройка-caddy-для-https)
13. [Шаг 11: Настройка Firewall](#шаг-11-настройка-firewall)
14. [Шаг 12: Проверка и перезагрузка](#шаг-12-проверка-и-перезагрузка)
15. [Шаг 13: Настройка быстрого деплоя](#шаг-13-настройка-быстрого-деплоя)
16. [Полезные команды](#полезные-команды)
17. [Решение проблем](#решение-проблем)

---

## Обзор

Данная инструкция описывает полное развертывание сервера **zakaz3.tomica.ru** с нуля на чистой Ubuntu 24.04 LTS.

### Архитектура

```
Internet → Caddy (HTTPS:443) → Next.js (localhost:3000) → Supabase (supabase.tomica.ru:8000)
```

### Компоненты

- **OS:** Ubuntu 24.04.3 LTS
- **Node.js:** v24.11.1 (через nvm)
- **PM2:** v6.0.14
- **Docker:** v29.1.1
- **Caddy:** latest (в Docker)
- **Next.js:** 15.5.4
- **Supabase:** External (на production сервере)

---

## Требования

### Системные требования

- **CPU:** 32 cores (текущая конфигурация)
- **RAM:** 61 GB (текущая конфигурация)
- **Disk:** 2 TB
- **OS:** Ubuntu 24.04 LTS

### Доступы

- **SSH:** makarenko@ai.tomica.ru
- **IP:** 78.140.55.90
- **Domain:** zakaz3.tomica.ru (DNS должен быть настроен)

### Необходимые данные

- Supabase Service Role Key
- Supabase Anon Key
- Доступ к GitHub репозиторию

---

## Шаг 1: Обновление системы

Подключитесь к серверу по SSH:

```bash
ssh makarenko@ai.tomica.ru
```

Обновите систему:

```bash
sudo apt update && sudo apt upgrade -y
```

**Время выполнения:** ~5 минут

---

## Шаг 2: Установка Node.js через nvm

### 2.1 Установка базовых инструментов

```bash
sudo apt install -y curl git build-essential
```

### 2.2 Установка nvm

```bash
curl -o- https://raw.githubusercontent.com/nvm-sh/nvm/v0.40.0/install.sh | bash
```

### 2.3 Загрузка nvm в текущую сессию

```bash
export NVM_DIR="$HOME/.nvm"
[ -s "$NVM_DIR/nvm.sh" ] && \. "$NVM_DIR/nvm.sh"
[ -s "$NVM_DIR/bash_completion" ] && \. "$NVM_DIR/bash_completion"
```

### 2.4 Установка Node.js v24

```bash
nvm install 24
```

### 2.5 Проверка установки

```bash
node --version  # Должно вывести: v24.11.1
npm --version   # Должно вывести: 11.6.2
```

**Время выполнения:** ~3 минуты

---

## Шаг 3: Установка PM2

```bash
npm install -g pm2
```

Проверка:

```bash
pm2 --version  # Должно вывести: 6.0.14
```

**Время выполнения:** ~1 минута

---

## Шаг 4: Установка Git

Git уже установлен на шаге 2.1. Проверка:

```bash
git --version  # Должно вывести: git version 2.43.0 или новее
```

---

## Шаг 5: Установка Docker

### 5.1 Установка Docker

```bash
curl -fsSL https://get.docker.com -o get-docker.sh
sudo sh get-docker.sh
```

### 5.2 Добавление пользователя в группу docker

```bash
sudo usermod -aG docker $USER
```

### 5.3 Проверка

```bash
docker --version  # Должно вывести: Docker version 29.1.1
```

### 5.4 Перелогиньтесь

Выйдите и зайдите снова по SSH, чтобы изменения вступили в силу:

```bash
exit
ssh makarenko@ai.tomica.ru
```

**Время выполнения:** ~3 минуты

---

## Шаг 6: Клонирование проекта

### 6.1 Создание директории

```bash
mkdir -p ~/projects
cd ~/projects
```

### 6.2 Клонирование репозитория

```bash
git clone https://github.com/Makarenko444/zakaz-3.git
cd zakaz-3
```

**Время выполнения:** ~1 минута

---

## Шаг 7: Настройка переменных окружения

### 7.1 Создание файла .env.local

```bash
cat > .env.local << 'EOF'
# Supabase Configuration
SUPABASE_SERVICE_ROLE_KEY=your_service_role_key_here
SUPABASE_DIRECT_URL=http://supabase.tomica.ru:8000

# Public keys
NEXT_PUBLIC_SUPABASE_URL=http://supabase.tomica.ru:8000
NEXT_PUBLIC_SUPABASE_ANON_KEY=your_anon_key_here

# Upload directory
UPLOAD_DIR=/home/makarenko/zakaz-3-uploads
EOF
```

**ВАЖНО:** Замените `your_service_role_key_here` и `your_anon_key_here` на реальные ключи из Supabase.

### 7.2 Создание директории для загрузок

```bash
mkdir -p ~/zakaz-3-uploads
```

### 7.3 Проверка

```bash
cat .env.local
```

---

## Шаг 8: Установка зависимостей и сборка

### 8.1 Установка зависимостей

```bash
npm install
```

**Время выполнения:** ~30 секунд

### 8.2 Сборка проекта

```bash
npm run build
```

**Время выполнения:** ~10 секунд

---

## Шаг 9: Запуск через PM2

### 9.1 Запуск приложения

```bash
pm2 start npm --name "zakaz-3" -- start
```

### 9.2 Проверка статуса

```bash
pm2 list
pm2 logs zakaz-3 --lines 20
```

Должно быть:

```
✓ Ready in 810ms
Local:        http://localhost:3000
Network:      http://78.140.55.90:3000
```

### 9.3 Сохранение конфигурации

```bash
pm2 save
```

### 9.4 Настройка автозапуска

```bash
pm2 startup
```

Скопируйте и выполните команду, которую выдаст PM2. Она будет выглядеть примерно так:

```bash
sudo env PATH=$PATH:/home/makarenko/.nvm/versions/node/v24.11.1/bin /home/makarenko/.nvm/versions/node/v24.11.1/lib/node_modules/pm2/bin/pm2 startup systemd -u makarenko --hp /home/makarenko
```

**Время выполнения:** ~2 минуты

---

## Шаг 10: Настройка Caddy для HTTPS

### 10.1 Создание Caddyfile

```bash
cd ~
cat > Caddyfile << 'EOF'
zakaz3.tomica.ru {
    encode zstd gzip
    reverse_proxy localhost:3000
}
EOF
```

### 10.2 Запуск Caddy в Docker

```bash
docker run -d \
  --name caddy \
  --restart unless-stopped \
  --network host \
  -v ~/Caddyfile:/etc/caddy/Caddyfile \
  -v caddy_data:/data \
  -v caddy_config:/config \
  caddy:latest
```

### 10.3 Проверка логов

```bash
docker logs caddy --tail 30
```

Дождитесь сообщения:

```
"certificate obtained successfully"
```

Это означает, что SSL сертификат от Let's Encrypt получен успешно.

**Время выполнения:** ~2 минуты (получение SSL сертификата)

---

## Шаг 11: Настройка Firewall

### 11.1 Открытие портов

```bash
# SSH (обязательно!)
sudo ufw allow 22/tcp

# HTTP и HTTPS для Caddy
sudo ufw allow 80/tcp
sudo ufw allow 443/tcp
```

### 11.2 Активация UFW

```bash
sudo ufw --force enable
```

### 11.3 Проверка статуса

```bash
sudo ufw status
```

Должно быть:

```
Status: active

To                         Action      From
--                         ------      ----
22/tcp                     ALLOW       Anywhere
80/tcp                     ALLOW       Anywhere
443/tcp                    ALLOW       Anywhere
```

**Время выполнения:** ~1 минута

---

## Шаг 12: Проверка и перезагрузка

### 12.1 Проверка локального доступа

```bash
curl -I http://localhost:3000
```

Должно вернуть HTTP 307 с редиректом на `/login`.

### 12.2 Проверка в браузере

Откройте в браузере:

```
https://zakaz3.tomica.ru
```

Должна открыться страница входа в систему.

### 12.3 Перезагрузка сервера

Перезагрузите сервер, чтобы проверить автозапуск:

```bash
sudo reboot
```

### 12.4 Проверка после перезагрузки

Через 1-2 минуты снова подключитесь:

```bash
ssh makarenko@ai.tomica.ru
```

Проверьте статус:

```bash
pm2 list
docker ps
```

Оба сервиса должны быть в статусе `online` / `Up`.

Откройте снова https://zakaz3.tomica.ru - сайт должен работать.

**Время выполнения:** ~5 минут

---

## Шаг 13: Настройка быстрого деплоя

### 13.1 Создание алиаса

```bash
echo "alias pr-zakaz3='cd ~/projects/zakaz-3 && ./pr-zakaz3.sh'" >> ~/.bashrc
source ~/.bashrc
```

### 13.2 Использование

Теперь для обновления проекта достаточно выполнить:

```bash
pr-zakaz3
```

Скрипт автоматически:
1. Получит изменения из GitHub (main)
2. Установит зависимости
3. Очистит старый билд
4. Соберет проект
5. Перезапустит PM2

---

## Полезные команды

### PM2

```bash
# Список процессов
pm2 list

# Логи приложения
pm2 logs zakaz-3
pm2 logs zakaz-3 --lines 100

# Перезапуск
pm2 restart zakaz-3

# Остановка
pm2 stop zakaz-3

# Удаление процесса
pm2 delete zakaz-3

# Мониторинг
pm2 monit
```

### Docker (Caddy)

```bash
# Статус контейнера
docker ps

# Логи Caddy
docker logs caddy
docker logs caddy --tail 50
docker logs caddy --follow

# Перезапуск Caddy
docker restart caddy

# Остановка и удаление
docker stop caddy
docker rm caddy

# Проверка сертификата
docker exec caddy caddy list-certificates
```

### Firewall

```bash
# Статус
sudo ufw status

# Разрешить порт
sudo ufw allow 8080/tcp

# Запретить порт
sudo ufw deny 8080/tcp

# Отключить firewall
sudo ufw disable
```

### Git

```bash
# Обновление кода
cd ~/projects/zakaz-3
git pull origin main

# Просмотр изменений
git status
git log --oneline -10

# Смена ветки
git checkout branch-name
```

### Системные

```bash
# Использование диска
df -h

# Использование памяти
free -h

# Загрузка CPU
htop

# Проверка портов
netstat -tulpn | grep LISTEN

# Перезагрузка
sudo reboot
```

---

## Решение проблем

### Проблема 1: Сайт не открывается (502 Bad Gateway)

**Причина:** Next.js не запущен или Caddy не может к нему подключиться.

**Решение:**

```bash
# Проверьте статус PM2
pm2 list

# Если offline, перезапустите
pm2 restart zakaz-3

# Проверьте логи
pm2 logs zakaz-3

# Проверьте, что Next.js слушает порт 3000
curl -I http://localhost:3000
```

### Проблема 2: PM2 не запускается после перезагрузки

**Причина:** Автозапуск не настроен.

**Решение:**

```bash
# Настройте startup
pm2 startup

# Выполните команду, которую выдаст PM2

# Сохраните конфигурацию
pm2 save
```

### Проблема 3: Caddy не получает SSL сертификат

**Причина:** DNS не настроен или порты 80/443 закрыты.

**Решение:**

```bash
# Проверьте DNS
dig zakaz3.tomica.ru

# Проверьте firewall
sudo ufw status

# Проверьте логи Caddy
docker logs caddy --tail 100
```

### Проблема 4: Node.js не найден после перелогина

**Причина:** nvm не загружен в новой сессии.

**Решение:**

```bash
# Загрузите nvm
export NVM_DIR="$HOME/.nvm"
[ -s "$NVM_DIR/nvm.sh" ] && \. "$NVM_DIR/nvm.sh"

# Или перезапустите shell
exec bash
```

### Проблема 5: Ошибка подключения к Supabase

**Причина:** Неправильные ключи или Supabase недоступен.

**Решение:**

```bash
# Проверьте .env.local
cat ~/projects/zakaz-3/.env.local

# Проверьте доступность Supabase
curl -I http://supabase.tomica.ru:8000

# Проверьте логи приложения
pm2 logs zakaz-3
```

### Проблема 6: Permission denied при запуске Docker

**Причина:** Пользователь не в группе docker.

**Решение:**

```bash
# Добавьте пользователя в группу
sudo usermod -aG docker $USER

# Перелогиньтесь
exit
ssh makarenko@ai.tomica.ru
```

---

## Контрольный список развертывания

- [ ] Ubuntu обновлена
- [ ] Node.js v24 установлен через nvm
- [ ] PM2 установлен глобально
- [ ] Git установлен
- [ ] Docker установлен
- [ ] Проект склонирован в ~/projects/zakaz-3
- [ ] .env.local создан с правильными ключами
- [ ] npm install выполнен успешно
- [ ] npm run build выполнен успешно
- [ ] PM2 запущен (zakaz-3 online)
- [ ] PM2 startup настроен
- [ ] Caddy запущен в Docker
- [ ] SSL сертификат получен
- [ ] UFW настроен (порты 22, 80, 443)
- [ ] Сайт открывается https://zakaz3.tomica.ru
- [ ] Сервер перезагружен и всё автоматически запустилось
- [ ] Алиас pr-zakaz3 настроен

---

## Заметки

### Различия между zakaz2 и zakaz3

- **zakaz2.tomica.ru** - Production сервер (боевой)
  - IP: 78.140.57.33
  - Пользователь: makarenko
  - Директория: ~/zakaz-3
  - PM2 процесс: zakaz-3
  - Также хостит Supabase

- **zakaz3.tomica.ru** - Development сервер (тестовый)
  - IP: 78.140.55.90
  - Пользователь: makarenko
  - Директория: ~/projects/zakaz-3
  - PM2 процесс: zakaz-3
  - Подключается к Supabase на zakaz2

### Обновления

Для обновления проекта используйте:

```bash
pr-zakaz3
```

Или вручную:

```bash
cd ~/projects/zakaz-3
git pull origin main
npm install
rm -rf .next
npm run build
pm2 restart zakaz-3
```

---

## История изменений

### 2025-12-02

- Первоначальное развертывание сервера zakaz3.tomica.ru
- Ubuntu 24.04.3 LTS
- Node.js v24.11.1
- PM2 v6.0.14
- Docker v29.1.1
- Caddy latest
- SSL сертификат получен от Let's Encrypt

---

**Документ составлен:** 2 декабря 2025
**Автор:** Claude (AI Assistant)
**Проверено на:** zakaz3.tomica.ru (78.140.55.90)
