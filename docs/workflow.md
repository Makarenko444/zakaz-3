# Схема работы над проектом Zakaz-3

## 🏗️ Три кодовые площадки

### 1. **GitHub** - Центральное хранилище кода
**Роль:** Единый источник правды (Single Source of Truth)

**Функции:**
- Хранение всего кода проекта
- Управление версиями (Git)
- Ветки разработки (main, feature branches)
- История изменений
- Pull Requests для code review
- CI/CD (при необходимости)

**Основные ветки:**
- `main` - стабильная ветка для production деплоя
- `claude/*` - feature ветки для разработки новых функций

---

### 2. **Claude Dev Server** - Площадка разработки
**Роль:** Разработка и тестирование

**Директория:** `/home/user/zakaz-3`

**Функции:**
- ✅ Написание кода
- ✅ Локальное тестирование (`npm run dev`)
- ✅ Проверка сборки (`npm run build`)
- ✅ Запуск линтера (`npm run lint`)
- ✅ Коммиты в feature ветки
- ✅ Push в GitHub в свои ветки (`claude/*`)
- ❌ **НЕ может** пушить напрямую в `main` (безопасность)

**Рабочий процесс Claude:**
```bash
# 1. Работаю в своей feature ветке
git checkout claude/feature-name

# 2. Пишу код и тестирую
npm run dev
npm run build

# 3. Коммичу изменения
git add .
git commit -m "Описание изменений"

# 4. Пушу в свою ветку на GitHub
git push -u origin claude/feature-name
```

---

### 3. **Production Server** - Боевой сервер
**Роль:** Запуск приложения для пользователей

**Пользователь:** makarenko
**Директория:** `~/zakaz-3`

**Функции:**
- ✅ Получение кода из GitHub (`git pull`)
- ✅ Мерж feature веток в `main`
- ✅ Push в `main` на GitHub
- ✅ Сборка production билда (`npm run build`)
- ✅ Запуск приложения (`pm2 restart zakaz-3`)
- ✅ Выполнение миграций БД
- ❌ **НЕ пишет** код напрямую

**Рабочий процесс деплоя:**
```bash
# 1. Перейти в директорию проекта
cd ~/zakaz-3

# 2. Убедиться что на ветке main
git checkout main

# 3. Получить изменения из feature ветки Claude
git pull origin claude/feature-name

# 4. Запушить в main на GitHub
git push origin main

# 5. Установить зависимости (если package.json изменился)
npm install

# 6. Собрать production билд
npm run build

# 7. Перезапустить приложение
pm2 restart zakaz-3
```

---

## 🔄 Полный цикл разработки

### Схема взаимодействия:

```
┌─────────────────────────────────────────────────────────────┐
│                                                             │
│  1. Claude Dev Server                                       │
│     - Пишет код                                             │
│     - Тестирует локально                                    │
│     - Коммитит в claude/* ветку                            │
│     └─> git push origin claude/feature-name                 │
│                                                             │
└──────────────────────┬──────────────────────────────────────┘
                       │
                       ▼
┌─────────────────────────────────────────────────────────────┐
│                                                             │
│  2. GitHub                                                  │
│     - Принимает push от Claude                              │
│     - Хранит claude/* ветку                                 │
│     - Доступен для Production Server                        │
│                                                             │
└──────────────────────┬──────────────────────────────────────┘
                       │
                       ▼
┌─────────────────────────────────────────────────────────────┐
│                                                             │
│  3. Production Server (makarenko)                           │
│     - git pull origin claude/feature-name                   │
│     - Проверяет изменения                                   │
│     - git push origin main                                  │
│     - npm run build                                         │
│     - pm2 restart zakaz-3                                   │
│                                                             │
└─────────────────────────────────────────────────────────────┘
```

---

## 📝 Правила и принципы

### ✅ DO (Делать)

**На Claude Dev Server:**
- Писать весь код
- Тестировать изменения локально
- Коммитить часто с понятными сообщениями
- Пушить только в свои `claude/*` ветки
- Исправлять ошибки TypeScript/ESLint до коммита

**На Production Server:**
- Получать код только через `git pull`
- Мержить feature ветки в `main`
- Пушить в `main` на GitHub
- Выполнять миграции БД
- Собирать и перезапускать приложение

**На GitHub:**
- Хранить всю историю изменений
- Использовать осмысленные commit messages
- Следовать стандартам именования веток

---

### ❌ DON'T (Не делать)

**На Claude Dev Server:**
- ❌ НЕ пушить напрямую в `main` (нет прав)
- ❌ НЕ запускать production команды

**На Production Server:**
- ❌ НЕ писать код напрямую
- ❌ НЕ делать коммиты с изменениями кода
- ❌ НЕ работать в feature ветках
- ❌ Только получать готовый код и деплоить

---

## 🚀 Типичные сценарии

### Сценарий 1: Новая функция

**Claude:**
```bash
# Создаю feature ветку (если нужно)
git checkout -b claude/new-feature

# Пишу код
# ... редактирую файлы ...

# Тестирую
npm run build

# Коммичу
git add .
git commit -m "Добавлена новая функция X"

# Пушу в GitHub
git push -u origin claude/new-feature
```

**Makarenko на Production:**
```bash
cd ~/zakaz-3
git checkout main
git pull origin claude/new-feature
git push origin main
npm install
npm run build
pm2 restart zakaz-3
```

---

### Сценарий 2: Исправление бага

**Claude:**
```bash
# Работаю в существующей ветке
git checkout claude/bugfix

# Исправляю баг
# ... правлю файлы ...

# Тестирую
npm run build

# Коммичу
git add .
git commit -m "Исправлен баг с авторизацией"

# Пушу
git push origin claude/bugfix
```

**Makarenko на Production:**
```bash
cd ~/zakaz-3
git pull origin claude/bugfix
git push origin main
npm run build
pm2 restart zakaz-3
```

---

### Сценарий 3: Миграция БД

**Claude:**
```bash
# Создаю файл миграции
echo "..." > database/migrations/009_new_table.sql

# Коммичу
git add database/migrations/009_new_table.sql
git commit -m "Добавлена миграция для новой таблицы"
git push origin claude/db-migration
```

**Makarenko на Production:**
```bash
# Получаю код
cd ~/zakaz-3
git pull origin claude/db-migration
git push origin main

# Выполняю миграцию
cat database/migrations/009_new_table.sql | docker exec -i supabase-db psql -U postgres

# Деплою приложение
npm run build
pm2 restart zakaz-3
```

---

## 📊 Преимущества этой схемы

1. **Безопасность** - Production сервер никогда не содержит незавершенного кода
2. **Проверка** - Весь код проходит тестирование на Dev Server
3. **История** - GitHub хранит полную историю изменений
4. **Откат** - Легко откатиться к предыдущей версии через Git
5. **Изоляция** - Dev и Production изолированы друг от друга
6. **Контроль** - Финальное решение о деплое принимает владелец Production

---

## 🔧 Инструменты и технологии

- **Git** - Система контроля версий
- **GitHub** - Хостинг Git репозиториев
- **npm** - Управление зависимостями Node.js
- **Next.js** - Фреймворк для сборки и разработки
- **PM2** - Process manager для Node.js на Production
- **PostgreSQL** - База данных (Supabase)
- **Docker** - Контейнеризация Supabase

---

## 📞 Коммуникация

**Формат взаимодействия:**
1. Claude разрабатывает функцию
2. Claude пушит в feature ветку
3. Claude сообщает в чате: "Готово! Выполните команды:"
4. Makarenko копирует команды и выполняет на Production
5. Makarenko сообщает результат (успех/ошибки)
6. При ошибках - Claude исправляет и повторяет процесс

**Обратная связь:**
- Всегда показывать вывод команд (особенно ошибки)
- Сообщать о нестандартном поведении
- Задавать вопросы при непонятных инструкциях

---

## ✨ Итог

Эта схема обеспечивает:
- ✅ Чистое разделение ответственности
- ✅ Безопасность production окружения
- ✅ Простоту отката изменений
- ✅ Полную историю разработки
- ✅ Минимум ручной работы

**Главное правило:** Production сервер только деплоит, Dev сервер только разрабатывает, GitHub хранит всё.
