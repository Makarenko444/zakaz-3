# Supabase Configuration
# Скопируйте этот файл в .env.local и заполните реальными значениями

# Private Supabase Service Role Key (используется только на сервере)
# Получите из файла /home/makarenko/directus/supabase/docker/.env на production
SUPABASE_SERVICE_ROLE_KEY=your_service_role_key_here

# Direct Supabase URL (для server-side операций через Kong API Gateway)
# На production используйте localhost:8000
# На локальной разработке может потребоваться внешний IP
SUPABASE_DIRECT_URL=http://localhost:8000

# Yandex Geosuggest API Key (для подсказок адресов)
# Получить можно на https://yandex.ru/maps-api/
# Бесплатный тариф - активировать через maps-api@support.yandex.ru
YANDEX_GEOSUGGEST_API_KEY=your_yandex_api_key_here

# ===== Синхронизация файлов между серверами =====

# URL peer-сервера для синхронизации файлов
# На zakaz2.tomica.ru указывает на zakaz3, и наоборот
# Пример для zakaz2: PEER_SERVER_URL=https://zakaz3.tomica.ru
# Пример для zakaz3: PEER_SERVER_URL=https://zakaz2.tomica.ru
PEER_SERVER_URL=

# Секретный ключ для межсерверной авторизации (должен быть одинаковым на обоих серверах)
# Сгенерировать: openssl rand -hex 32
FILE_SYNC_SECRET=
