# Миграция 028: Нормализация адресов и узлов

**Дата**: 27 ноября 2025
**Статус**: Готова к применению
**Зависимости**: Миграции 024, 025, 026, 027

## Описание проблемы

### Текущая ситуация (до миграции):

В таблице `zakaz_nodes` хранятся и узлы связи (оборудование), и адреса. При импорте узлов из Excel создаются **дубликаты адресов**:

```
id=1, code=ПП1869-1, city=Томск, street=Сергея Лазо, house=4/2
id=2, code=ПП1869-2, city=Томск, street=Сергея Лазо, house=4/2  ← тот же адрес!
id=3, code=АО1372,   city=Томск, street=Сергея Лазо, house=4/2  ← тот же адрес!
```

### Проблемы:

1. **Дублирование данных**: Один адрес хранится многократно
2. **Ошибки привязки**: При создании заявки пользователь видит 3 записи с одинаковым адресом и может выбрать любую
3. **Сложность обслуживания**: Изменение адреса требует обновления всех дубликатов
4. **Несоответствие реальности**: На одном адресе может быть несколько узлов, но адрес - один

## Решение

Разделить данные на две таблицы:

### `zakaz_addresses` - Справочник уникальных адресов

| Поле | Тип | Описание |
|------|-----|----------|
| `id` | UUID | Первичный ключ |
| `city` | VARCHAR(200) | Город (обязательно) |
| `street` | VARCHAR(500) | Улица |
| `house` | VARCHAR(50) | Номер дома |
| `building` | VARCHAR(50) | Строение/корпус |
| `address` | TEXT | Полный адрес (автоформируется) |
| `comment` | TEXT | Комментарий к адресу |
| `created_at` | TIMESTAMP | Дата создания |
| `updated_at` | TIMESTAMP | Дата обновления |

**Уникальность**: `(city, street, house, building)` - один адрес в системе только один раз

### `zakaz_nodes` - Узлы связи (оборудование)

Изменения:
- ✅ **Добавлено**: `address_id` (UUID, FK → zakaz_addresses.id)
- ❌ **Удалено**: `city`, `street`, `house`, `building`, `address`, `comment`
- 🔄 **Сохранено**: `code`, `node_type`, `presence_type`, `location_details`, `comm_info`

### `zakaz_applications` - Заявки

Изменения:
- ✅ **Добавлено**: `address_id` (UUID, FK → zakaz_addresses.id)
- 🔄 **Сохранено**: `node_id` (для обратной совместимости)

## Этапы миграции

### Этап 1: Создание таблицы zakaz_addresses
- Создание таблицы с полями city, street, house, building, address, comment
- Уникальный индекс на (city, street, house, building)
- Индексы для быстрого поиска
- Полнотекстовые индексы (GIN) для поиска

### Этап 2: Функция форматирования адреса
- `format_address(city, street, house, building)` → "Томск, Сергея Лазо, 4/2"
- Триггер для автоматического обновления поля `address`
- **Примечание:** Изначально функция добавляла "д." перед номером дома, но это убрано в миграции 029

### Этап 3: Извлечение уникальных адресов
```sql
INSERT INTO zakaz_addresses (city, street, house, building, ...)
SELECT DISTINCT ON (city, street, house, building) ...
FROM zakaz_nodes
```

### Этап 4: Связь zakaz_nodes → zakaz_addresses
- Добавление `address_id` в zakaz_nodes
- Обновление `address_id` по совпадению (city, street, house, building)
- Создание FK с `ON DELETE RESTRICT`

### Этап 5: Связь zakaz_applications → zakaz_addresses
- Добавление `address_id` в zakaz_applications
- Обновление через `node_id`
- Создание FK с `ON DELETE SET NULL`

### Этап 6: Очистка zakaz_nodes
- Удаление триггера `trigger_update_node_address`
- Удаление ненужных индексов
- Удаление колонок: city, street, house, building, address, comment

### Этап 7: Обновление функций
- Пересоздание `get_applications_by_address_stats()` для работы с новой структурой

## Результат

### До миграции:
```
zakaz_nodes (184 записи):
  id=1, code=ПП1869-1, city=Томск, street=Сергея Лазо, house=4/2
  id=2, code=ПП1869-2, city=Томск, street=Сергея Лазо, house=4/2
  id=3, code=АО1372,   city=Томск, street=Сергея Лазо, house=4/2

zakaz_applications:
  id=101, node_id=1  ← может быть 1, 2 или 3 (неоднозначно!)
```

### После миграции:
```
zakaz_addresses (меньше записей, только уникальные адреса):
  id=a1, city=Томск, street=Сергея Лазо, house=4/2, address="Томск, Сергея Лазо, 4/2"

zakaz_nodes (184 записи):
  id=1, code=ПП1869-1, address_id=a1
  id=2, code=ПП1869-2, address_id=a1
  id=3, code=АО1372,   address_id=a1

zakaz_applications:
  id=101, address_id=a1, node_id=1  ← привязка к адресу однозначна!
```

## Влияние на код приложения

### Обязательные изменения:

1. **TypeScript типы** (`lib/types.ts`):
   ```typescript
   export interface Address {
     id: string
     city: string
     street: string | null
     house: string | null
     building: string | null
     address: string
     comment: string | null
     created_at: string
     updated_at: string
   }

   export interface Node {
     id: string
     code: string
     address_id: string  // ← новое поле
     // Удалены: city, street, house, building, address, comment
     node_type: NodeType
     presence_type: PresenceType
     location_details: string | null
     comm_info: string | null
     // ...остальные поля
   }

   export interface Application {
     id: string
     address_id: string | null  // ← новое поле
     node_id: string | null     // ← сохранено для совместимости
     // ...остальные поля
   }
   ```

2. **API импорта узлов** (`app/api/nodes/import/route.ts`):
   - Сначала создавать/искать адрес в `zakaz_addresses`
   - Потом создавать узел с `address_id`

3. **API адресов** (новый):
   - `GET /api/addresses` - список адресов с поиском
   - `POST /api/addresses` - создание адреса
   - `GET /api/addresses/[id]` - детали адреса
   - `PATCH /api/addresses/[id]` - редактирование
   - `DELETE /api/addresses/[id]` - удаление (если нет ссылок)

4. **API узлов** (`app/api/nodes/*`):
   - JOIN с `zakaz_addresses` для получения адреса
   - Фильтрация по адресу через `address_id`

5. **Компоненты интерфейса**:
   - Мастер привязки адреса: выбор из `zakaz_addresses`, а не из `zakaz_nodes`
   - Страница узлов: отображение адреса через JOIN
   - Форма создания заявки: привязка к `zakaz_addresses`

## Обратная совместимость

✅ **Сохранено**: `node_id` в `zakaz_applications` не удалено
✅ **Миграция данных**: Все существующие заявки получат `address_id` автоматически
✅ **Откат**: Можно создать обратную миграцию (денормализация)

## Проверка после применения

```sql
-- 1. Проверить, что все узлы имеют address_id
SELECT COUNT(*) FROM zakaz_nodes WHERE address_id IS NULL;
-- Ожидается: 0

-- 2. Проверить количество уникальных адресов
SELECT COUNT(*) FROM zakaz_addresses;
-- Ожидается: меньше чем количество узлов

-- 3. Проверить, что заявки получили address_id
SELECT COUNT(*) FROM zakaz_applications WHERE node_id IS NOT NULL AND address_id IS NULL;
-- Ожидается: 0

-- 4. Проверить дубликаты адресов (их не должно быть)
SELECT city, street, house, building, COUNT(*)
FROM zakaz_addresses
GROUP BY city, street, house, building
HAVING COUNT(*) > 1;
-- Ожидается: 0 строк

-- 5. Проверить несколько узлов на одном адресе
SELECT a.address, COUNT(n.id) as nodes_count
FROM zakaz_addresses a
LEFT JOIN zakaz_nodes n ON n.address_id = a.id
GROUP BY a.id, a.address
HAVING COUNT(n.id) > 1
ORDER BY COUNT(n.id) DESC
LIMIT 10;
-- Покажет адреса с несколькими узлами
```

## Риски и митигация

| Риск | Вероятность | Митигация |
|------|-------------|-----------|
| Потеря данных адресов | Низкая | Миграция только переносит данные, не удаляет |
| Нарушение FK при импорте | Средняя | Сначала создавать адрес, потом узел |
| Длительное выполнение | Низкая | Таблицы небольшие (~200 записей) |
| Откат миграции | Средняя | Создать обратную миграцию 029_rollback_addresses |

## Следующие шаги

1. ✅ Применить миграцию на dev сервере (zakaz3.tomica.ru)
2. ✅ Обновить TypeScript типы
3. ✅ Обновить API импорта узлов
4. ✅ Создать API для работы с адресами
5. ✅ Обновить компоненты интерфейса
6. ✅ Протестировать импорт узлов
7. ✅ Протестировать создание заявок
8. ⏳ Применить на production сервере (zakaz2.tomica.ru)

## История

- **24.11.2025**: Миграция 024 - объединение zakaz_addresses и zakaz_nodes
- **27.11.2025**: Миграция 028 - **разделение обратно** для устранения дублирования адресов

## Автор

Makarenko444 + Claude Code
