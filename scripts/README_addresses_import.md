# Импорт адресов Томска в базу данных

Это руководство описывает различные способы заполнения таблицы `zakaz_addresses` адресами города Томска.

## Варианты загрузки адресов

### 🥇 Вариант 1: Автоматическая загрузка через КЛАДР API (Рекомендуется)

**Самый простой способ** - не требует регистрации и API ключей.

```bash
# 1. Установить зависимости
pip install requests psycopg2-binary python-dotenv

# 2. Убедиться что DATABASE_URL есть в .env.local
# DATABASE_URL=postgresql://user:password@host:port/database

# 3. Запустить импорт
python scripts/import-addresses.py --source kladr --city Томск

# 4. Или сначала посмотреть что загрузится (dry-run)
python scripts/import-addresses.py --source kladr --dry-run
```

**Результат:**
- ✅ Все улицы Томска
- ✅ Все номера домов
- ✅ Автоматическая загрузка через API
- ✅ Не требует регистрации

**Ожидаемое время:** 5-10 минут (зависит от скорости интернета)

---

### 🥈 Вариант 2: Загрузка тестовых адресов для разработки

Для быстрого старта можно загрузить ~100 основных адресов Томска:

```bash
# Выполнить SQL скрипт
psql $DATABASE_URL -f scripts/import-addresses-from-fias.sql

# Или через Supabase Dashboard:
# 1. Открыть SQL Editor
# 2. Скопировать содержимое scripts/import-addresses-from-fias.sql
# 3. Выполнить
```

**Результат:**
- ✅ ~100 популярных адресов центра Томска
- ✅ Готов к использованию сразу
- ⚠️ Неполный справочник (только центр)

---

### 🥉 Вариант 3: Импорт из CSV файла

Если у вас уже есть список адресов в Excel/CSV:

**Шаг 1:** Подготовить CSV файл в формате:
```csv
street,house,comment
"ул. Ленина","5",""
"ул. Ленина","7","Многоквартирный дом"
"пр. Фрунзе","10","Торговый центр"
```

**Шаг 2:** Импортировать:
```bash
python scripts/import-addresses.py --source csv --file my_addresses.csv
```

---

### 🏆 Вариант 4: Полная база ФИАС (для production)

**Для production системы** рекомендуется полная база ФИАС.

#### Способ 4А: Через готовый дамп (самый быстрый)

```bash
# 1. Скачать дамп для Томской области
# Готовые дампы: https://github.com/hflabs/fias_postgres

wget https://github.com/hflabs/fias_postgres/releases/download/latest/fias_tomsk.sql.gz

# 2. Распаковать
gunzip fias_tomsk.sql.gz

# 3. Импортировать нужные таблицы
psql $DATABASE_URL -f fias_tomsk.sql
```

#### Способ 4Б: Через GarLoader

GarLoader - инструмент для загрузки ФИАС в PostgreSQL.

```bash
# 1. Установить GarLoader
git clone https://github.com/YPermitin/GarLoader
cd GarLoader

# 2. Настроить конфигурацию (только Томская область)
# Отредактировать appsettings.json

# 3. Запустить импорт
dotnet run

# 4. Экспортировать в нужный формат
# SQL скрипт для конвертации ФИАС → zakaz_addresses
```

---

## Сравнение методов

| Метод | Адресов | Время | Сложность | Рекомендация |
|-------|---------|-------|-----------|--------------|
| КЛАДР API | ~50,000 | 10 мин | ⭐ Легко | **Для старта** |
| Тестовые данные | ~100 | 1 мин | ⭐ Легко | Только для разработки |
| CSV | Любое | 1 мин | ⭐⭐ Средне | Если есть готовый список |
| ФИАС полный | ~200,000+ | 30+ мин | ⭐⭐⭐ Сложно | Для production |

---

## Проверка результата

После импорта проверьте данные:

```sql
-- Общая статистика
SELECT
  COUNT(*) as total_addresses,
  COUNT(DISTINCT street) as unique_streets
FROM zakaz_addresses;

-- Топ-10 улиц по количеству домов
SELECT street, COUNT(*) as houses
FROM zakaz_addresses
GROUP BY street
ORDER BY houses DESC
LIMIT 10;

-- Проверка поиска
SELECT * FROM search_addresses_fuzzy('Ленина 5');
```

---

## Troubleshooting

### Ошибка: "DATABASE_URL not found"

Добавьте в `.env.local`:
```
DATABASE_URL=postgresql://user:password@host:port/database
```

### Ошибка: "psycopg2 not found"

```bash
pip install psycopg2-binary
```

### Ошибка: "requests module not found"

```bash
pip install requests
```

### КЛАДР API не отвечает

Попробуйте альтернативный метод (CSV или тестовые данные).

---

## Обновление адресов

Для периодического обновления справочника:

```bash
# 1. Загрузить новые адреса (дубликаты будут пропущены)
python scripts/import-addresses.py --source kladr

# 2. Удалить устаревшие адреса вручную через SQL
DELETE FROM zakaz_addresses WHERE street = 'старая улица';
```

---

## Полезные ссылки

- **КЛАДР API:** http://kladr-api.ru/
- **ФИАС официальный:** https://fias.nalog.ru/
- **2GIS API:** https://dev.2gis.ru/
- **GarLoader:** https://github.com/YPermitin/GarLoader
- **OpenStreetMap Томск:** https://www.openstreetmap.org/relation/140294

---

## Лицензия данных

- **КЛАДР:** Свободное использование для некоммерческих целей
- **ФИАС:** Официальные государственные данные, открытая лицензия
- **2GIS:** Требуется регистрация и соблюдение условий использования
- **OpenStreetMap:** Open Database License (ODbL)

---

## Вопросы?

Если возникли проблемы с импортом адресов, создайте issue в репозитории проекта.
