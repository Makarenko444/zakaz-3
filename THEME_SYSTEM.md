# Система тем в стиле Битрикс-24

## Обзор

В проект добавлена система тем, вдохновленная дизайном Битрикс-24. Каждый сотрудник может выбирать между светлой и темной темой, выбор сохраняется в localStorage браузера.

## Возможности

### 🎨 Два режима темы
- **Светлая тема** - чистый, профессиональный дизайн с яркими цветами
- **Темная тема** - комфортная для глаз темная цветовая схема

### 💾 Автоматическое сохранение
- Выбор темы сохраняется в localStorage
- При следующем посещении применяется сохраненная тема
- Если тема не выбрана, используется системные настройки браузера

### 🎯 Цветовая палитра в стиле Битрикс-24
- Акцентный цвет: `#2fc6f6` (бирюзовый)
- Статусные цвета:
  - Успех: `#55d88a` (зеленый)
  - Предупреждение: `#ffa826` (оранжевый)
  - Опасность: `#ff5752` (красный)
  - Информация: `#2fc6f6` (бирюзовый)

### ⚡ Плавные переходы
- Анимированное переключение между темами
- Анимация иконок при наведении
- Плавные переходы цветов

## Использование

### Добавление переключателя темы

Импортируйте компонент `ThemeToggle` в любую часть вашего приложения:

```tsx
import ThemeToggle from "@/components/ThemeToggle";

export default function Header() {
  return (
    <header>
      {/* Ваш контент */}
      <ThemeToggle />
    </header>
  );
}
```

### Использование хука useTheme

Для программного управления темой используйте хук `useTheme`:

```tsx
"use client";

import { useTheme } from "@/contexts/ThemeContext";

export default function MyComponent() {
  const { theme, toggleTheme, setTheme } = useTheme();

  return (
    <div>
      <p>Текущая тема: {theme}</p>
      <button onClick={toggleTheme}>Переключить тему</button>
      <button onClick={() => setTheme("light")}>Светлая</button>
      <button onClick={() => setTheme("dark")}>Темная</button>
    </div>
  );
}
```

### Использование CSS переменных

Все компоненты должны использовать CSS переменные для цветов:

```css
.my-component {
  background: var(--surface);
  color: var(--foreground);
  border: 1px solid var(--border-color);
}

.my-button {
  background: var(--accent);
  color: #fff;
}

.my-button:hover {
  background: var(--accent-hover);
}
```

## Доступные CSS переменные

### Основные цвета
- `--background` - Фон приложения
- `--foreground` - Основной цвет текста
- `--surface` - Фон карточек и панелей
- `--surface-strong` - Более контрастный фон
- `--surface-hover` - Фон при наведении

### Границы
- `--border-color` - Основной цвет границ
- `--border-light` - Светлые границы

### Акценты
- `--accent` - Основной акцентный цвет
- `--accent-hover` - Акцент при наведении
- `--accent-soft` - Полупрозрачный акцент
- `--accent-strong` - Более насыщенный акцент

### Статусы
- `--success` / `--success-soft` - Зеленый (успех)
- `--warning` / `--warning-soft` - Оранжевый (предупреждение)
- `--info` / `--info-soft` - Синий (информация)
- `--danger` / `--danger-soft` - Красный (опасность)

### Текст
- `--text-primary` - Основной текст
- `--text-secondary` - Вторичный текст
- `--text-muted` - Приглушенный текст
- `--text-disabled` - Отключенный текст

### Тени
- `--shadow-sm` - Маленькая тень
- `--shadow-md` - Средняя тень
- `--shadow-lg` - Большая тень
- `--shadow-hover` - Тень при наведении

### Радиусы
- `--radius-sm` - 6px
- `--radius-md` - 10px
- `--radius-lg` - 16px
- `--radius-full` - 9999px (круглые элементы)

## Структура файлов

```
/home/user/zakaz-3/
├── contexts/
│   └── ThemeContext.tsx       # Контекст и провайдер тем
├── components/
│   └── ThemeToggle.tsx        # Компонент переключателя
├── app/
│   ├── globals.css            # Глобальные стили с переменными тем
│   └── layout.tsx             # Корневой layout с ThemeProvider
└── THEME_SYSTEM.md            # Эта документация
```

## Примеры интеграции

### В дашборде

```tsx
"use client";

import ThemeToggle from "@/components/ThemeToggle";

export default function DashboardLayout() {
  return (
    <div className="dashboard">
      <header className="dashboard-header">
        <h1>Панель управления</h1>
        <ThemeToggle />
      </header>
      {/* Остальной контент */}
    </div>
  );
}
```

### В настройках профиля

```tsx
"use client";

import { useTheme } from "@/contexts/ThemeContext";

export default function ProfileSettings() {
  const { theme, setTheme } = useTheme();

  return (
    <div className="settings">
      <h2>Настройки отображения</h2>
      <div className="theme-selector">
        <label>
          <input
            type="radio"
            value="light"
            checked={theme === "light"}
            onChange={() => setTheme("light")}
          />
          Светлая тема
        </label>
        <label>
          <input
            type="radio"
            value="dark"
            checked={theme === "dark"}
            onChange={() => setTheme("dark")}
          />
          Темная тема
        </label>
      </div>
    </div>
  );
}
```

## Советы по дизайну

1. **Всегда используйте CSS переменные** вместо жестко заданных цветов
2. **Тестируйте в обеих темах** - убедитесь, что контент читаем в обеих темах
3. **Используйте семантические цвета** - `--success` для успешных операций, `--danger` для опасных
4. **Избегайте чистого белого/черного** - используйте `--surface-strong` и `--background`
5. **Проверяйте контрастность** - особенно для текста на цветных фонах

## Поддержка браузеров

Система тем поддерживается во всех современных браузерах:
- Chrome/Edge 88+
- Firefox 78+
- Safari 14+

## Будущие улучшения

- [ ] Дополнительные темы (синяя, зеленая и т.д.)
- [ ] Настройка акцентных цветов
- [ ] Синхронизация темы между устройствами (через API)
- [ ] Автоматическое переключение по времени суток
